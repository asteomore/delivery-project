[pytest]
DJANGO_SETTINGS_MODULE = delivery_service.settings
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts =
    --verbose
    --strict-markers
    --reuse-db
testpaths =
    delivery_service/tests
